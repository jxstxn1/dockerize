name: dockerize_server
description: A new Dockerized Flutter App

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dockerize_data: 
    path: ../../dockerize_data
  dockerize_sidekick_plugin: ^0.7.0
  shelf: ^1.4.0
  shelf_helmet: ^2.1.0
  shelf_router: ^1.1.3
  shelf_static: ^1.1.0

dev_dependencies: 
  lint: '>=2.1.0 <3.0.0'

format:
  line_length: 120