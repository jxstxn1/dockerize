name: dockerize_server
description: A new Dockerized Flutter App


publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  dockerize_sidekick_plugin: ">=0.5.0 <1.0.0"
  shelf: ^1.4.0
  shelf_helmet: ^1.1.0
  shelf_router: ^1.1.3
  shelf_static: ^1.1.0

dev_dependencies: 
  lint: '>=1.7.0 <2.0.0'

format:
  line_length: 120